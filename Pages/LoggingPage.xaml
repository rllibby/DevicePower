<Page
    x:Class="DevicePower.Pages.LoggingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevicePower.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Background="#FF2064B0" Foreground="White" RequestedTheme="Dark">

    <Grid Background="#FF2064B0">
        <ListBox x:Name="LogList" Margin="10,10,10,10" Background="#FF2064B0" BorderBrush="#FF2064B0" Foreground="White" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding}" Foreground="White" FontSize="14" LineStackingStrategy="BlockLineHeight" LineHeight="8" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
