<Page
    x:Class="DevicePower.Pages.LoggingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevicePower.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Dark">

    <Grid Background="{ThemeResource SystemControlBackgroundAccentBrush}">
        <ListView x:Name="LogList" Margin="10,10,10,10" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding}" Foreground="White" FontSize="14" LineStackingStrategy="BlockLineHeight" LineHeight="12" TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Left" TextLineBounds="TrimToBaseline" FontStretch="Condensed" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
